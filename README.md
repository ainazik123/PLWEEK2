# Java Collections Exercises Repository

This repository contains **21 Java exercises** demonstrating the use of **SET**, **STACK**, and **QUEUE**. Each exercise is implemented as a separate Java class named according to the task number (e.g., `Main.java`, `Main2.java`, ..., `Main21.java`).
